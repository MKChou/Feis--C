#include <stdio.h>

int main(){
    int v[5]={};
    int *n = v;   // &v[0]  == n
    n[0] = 0;     // a[b] == 就是 *(a+b), n[0]就是 *(n+0) , v[3]就是 v[0]+3 == *(v+3)
                  // 所以 v[3]==3[v] >>> *(0+3)==*(3+0) 
}